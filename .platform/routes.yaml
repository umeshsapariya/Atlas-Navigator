# The routes of the project.
#
# Each route describes how an incoming URL is going
# to be processed by Platform.sh.

"https://login.{default}/":
    type: upstream
    upstream: "app:http"

"https://luckcompanies.{default}/":
    type: upstream
    upstream: "app:http"

